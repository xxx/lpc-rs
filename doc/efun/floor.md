# floor

`float floor(int | float x)`

Return the largest whole number not above `x`, as a float: `floor(4.5)` is
`4.0`, `floor(-4.5)` is `-5.0` and `floor(5)` is `5.0`. An int argument is promoted; a non-number is an error.

### See also

`ceil`, `to_int`
