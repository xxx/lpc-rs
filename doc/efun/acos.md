# acos

`float acos(int | float x)`

Return the arc cosine of `x` in radians, in `0..pi`. An `x` outside
`-1..1` is an error. An int argument is promoted; a non-number is an error.

### See also

`cos`, `asin`, `atan`
