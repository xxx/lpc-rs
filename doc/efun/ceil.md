# ceil

`float ceil(int | float x)`

Return the smallest whole number not below `x`, as a float: `ceil(4.5)` is
`5.0`, `ceil(-4.5)` is `-4.0` and `ceil(4)` is `4.0`. An int argument is promoted; a non-number is an error.

### See also

`floor`, `to_int`
