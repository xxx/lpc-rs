# log

`float log(int | float x)`

Return the natural logarithm of `x`. An `x` that is not positive is an
error. An int argument is promoted; a non-number is an error.

### See also

`exp`, `pow`, `sqrt`
