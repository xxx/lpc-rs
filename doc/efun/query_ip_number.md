# query_ip_number

`string query_ip_number(object ob = this_player())`

The address of `ob`'s connection, in dotted-decimal or IPv6 form. Returns 0
when `ob` has no connection.

### See also

`query_connection`, `interactive`
