# asin

`float asin(int | float x)`

Return the arc sine of `x` in radians, in `-pi/2..pi/2`. An `x` outside
`-1..1` is an error. An int argument is promoted; a non-number is an error.

### See also

`sin`, `acos`, `atan`
