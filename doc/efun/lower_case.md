# lower_case

`string lower_case(string str)`

Return `str` with every character converted to lower case. Case mapping is
Unicode-aware: `lower_case("ÉCOLE")` is `"école"`.

### See also

`upper_case`, `capitalize`
