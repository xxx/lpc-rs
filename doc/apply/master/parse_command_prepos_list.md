# parse_command_prepos_list

`string *parse_command_prepos_list()`

The list `%p` matches against when the caller's destination holds no array;
entries may be several words, such as `in front of`. Optional; no list when
undefined.

### See also

`parse_command`
