# parse_command_all_word

`string parse_command_all_word()`

The word meaning every candidate, such as `all`; `%i`/`%l` report numeral 0
for it. Undefined means no such word.

### See also

`parse_command`
